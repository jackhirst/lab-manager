# lab-manager

Under construction.

Tools to plan, execute, and track wet lab experiments, managed via a simple GUI.

Currently runs via:
uv run -m lab_manager.scheduler

But will be updated in future.